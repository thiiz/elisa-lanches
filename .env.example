# Hygraph CMS Configuration
# Get these values from Hygraph: Project Settings > API Access

# Content API endpoint (public or private)
HYGRAPH_ENDPOINT=https://api-sa-east-1.hygraph.com/v2/YOUR_PROJECT_ID/master

# Content API token (public or private)
HYGRAPH_TOKEN=

# Secret for validating Webhook requests (Settings > Webhooks)
HYGRAPH_WEBHOOK_SECRET=
